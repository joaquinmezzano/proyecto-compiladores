program {
    bool general;
    bool value = true;
    bool p;

    bool dif_scope_def(bool p) {
        bool general;
        general = false;
        return general == p;
    }

    void main() {
        bool res = dif_scope_def(value);
        general = true;
        return ;
    }
}
