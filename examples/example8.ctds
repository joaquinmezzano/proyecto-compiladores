program {
    integer inc(integer x) {
        if (x < 5) then {
            inc(x+1);
        } else {
            return x;
        }
    }
    
    void main() {
        integer y = 0;
        inc(y);
    }
}